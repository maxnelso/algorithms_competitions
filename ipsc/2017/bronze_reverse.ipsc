jump 11 -> to beginning
print; jump 13
push 1; jump 8 -> pop;dup
push b; jump x -> pop;*
push a; jump 9 -> pop;dup
push -1; *;
pop
pop; *
pop; dup
jump -> some push
input; jump 10 -> jump to push c
input; jump 4 -> jump to push a
input; jump 11 -> jump to print
