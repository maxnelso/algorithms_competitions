#include <vector>
using namespace std;

class CandidatesSelectionEasy {
  public: vector<int> sortaaa(vector<string> score, int x);
};

vector<int> CandidatesSelectionEasy::sortaaa(vector<string> score, int x) {
  /*
  vector<int> out;
  for (int i = 0; i < score.size(); i++) {
    out.push_back(0);
  }
  vector<pair<int, int> > pairs;
  for (int i = 0; i < score.size(); i++) {
    pairs.push_back(make_pair(x, x));
  }
  std::sortaaa(pairs.begin(), pairs.end());
  for (int i = 0; i < score.size(); i++) {
   out[pairs[i].second] = i;
  }
  return out;
  */
  vector<int> v;
  return v;
}

int main() {
  return 0;
}
