/*
 ID: maxnelso1
 LANG: C++
 PROG: X
*/
#include <vector>
#include <list>
#include <map>
#include <set>
#include <deque>
#include <queue>
#include <stack>
#include <bitset>
#include <algorithm>
#include <functional>
#include <numeric>
#include <utility>
#include <sstream>
#include <iostream>
#include <iomanip>
#include <cstdio>
#include <cmath>
#include <cstdlib>
#include <cctype>
#include <string>
#include <cstring>
#include <ctime>
using namespace std;

#define ASSERT(X) {if (!(X)) {printf("\n assertion failed at line %d\n",__LINE__);exit(0);}}

int main(int argc, char* argv[]) {
  setbuf(stdout, NULL);
  freopen("X.in", "r", stdin);
  freopen("X.out", "w", stdout);
  int num_tests;
  scanf("%d", &num_tests);
  for (int case_id = 0; case_id < num_tests; case_id++) {

  }
  return(0);

}
